{
  "name": "family-tree-builder",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "client"
  ],
  "scripts": {
    "dev": "bash dev.sh",
    "dev:server": "cd server && mvn spring-boot:run",
    "dev:client": "npm run dev -w client -- --open",
    "test": "npm run test -w client && cd server && mvn test"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
